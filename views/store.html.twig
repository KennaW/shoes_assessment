<html>
    <head>
        <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css'>
        <title>{{ store.getStoreName }}</title>
    </head>
    <body>

        <h1>{{ store.getStoreName }}</h1>
        {% if brands is not empty %}
            {% for brand in brands %}
            <li><a href="/brands/{{ brand.getId }}">{{ brand.getBrandName }}</a></li>
            {% endfor %}
        {% endif %}

        <h4>Add a new brand for this store:</h4>

  <form action='/add_brand' method='post'>
      <input id="id" name="id" type="hidden" value="{{ store.getId }}">
      <input type="text" name="brand_name" id="brand_name">
      <button type='submit'>Add shoe brand</button>
  </form>

  <p><a href="/store/{{ store.getId }}/edit">Edit store name</a></p>

  <p><a href='/'>Home</a></p>

  <!-- <form action="/store/{{ store.getId }}" method="patch">
      <input name"_method" type="hidden" value="patch">

      <label for="store_name">Edit the store name</label>

      <input id="store_name" name="store_name" type="text">
      <button type="submit" class='btn'>Update</button>

  </form> -->
<!-- <p>
    <form action="/store/{{ store.getId }}" method="post">
        <input name="_method" type="hidden" value="delete">
        <button type="submit" class='btn btn-danger'>Delete this Store</button>
    </form>
</p> -->
    </body>
</html>
